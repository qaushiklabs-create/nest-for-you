name: Deploy to cPanel

on:
  push:
    branches:
      - main  # Trigger on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2  # Checks out the repository code

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.ssh_private_key }}  # Use the SSH private key you added to GitHub secrets

      - name: Deploy to cPanel
        run: |
          ssh lbm0yd8awsua@sg2plzcpnl509520.prod.sin2.secureserver.net 'cd /home/lbm0yd8awsua/public_html && git pull origin main'